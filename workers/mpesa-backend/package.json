{
  "name": "hostel-mpesa-backend",
  "version": "1.0.0",
  "private": true,
  "description": "M-Pesa (Daraja) STK push backend for the hostel booking system, running on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  }
}